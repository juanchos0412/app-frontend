---
import Layout from "../layouts/Layout.astro"; // Importa el componente Layout desde la ruta especificada
---

<Layout title="Admin"> <!-- Usamos el componente Layout con el título "Admin" -->
    <div class="list-container"> <!-- Contenedor principal para la lista de usuarios -->
        <ul class="user-list"> <!-- Lista desordenada que contiene los usuarios -->
            <li class="user-item"> <!-- Elemento de la lista para un usuario -->
                <span class="user-name">Grace Chen</span> <!-- Nombre del usuario -->
                <div class="container-buttons-list"> <!-- Contenedor para los botones de acción -->
                    <a href="#" class="user-link edit-link" data-user="Grace Chen"> <!-- Enlace para editar el usuario, incluye atributo data-user -->
                        Grace Chen ⚙️
                    </a>
                    <a href="#" class="delete">🗑️</a> <!-- Enlace para eliminar al usuario -->
                </div>
            </li>
            <li class="user-item"> <!-- Elemento de la lista para otro usuario -->
                <span class="user-name">Olivia Wang</span> <!-- Nombre del usuario -->
                <div class="container-buttons-list"> <!-- Contenedor para los botones de acción -->
                    <a href="#" class="user-link edit-link" data-user="Olivia Wang"> <!-- Enlace para editar el usuario -->
                        Olivia Wang ⚙️
                    </a>
                    <a href="#" class="delete">🗑️</a> <!-- Enlace para eliminar al usuario -->
                </div>
            </li>
            <!-- Agrega más usuarios según sea necesario -->
        </ul>

        <!-- Formulario para editar usuario -->
        <div id="edit-form-container" class="hidden"> <!-- Contenedor del formulario de edición, inicialmente oculto -->
            <h3>Edit User</h3> <!-- Encabezado del formulario de edición -->
            <form id="edit-form"> <!-- Formulario para editar los datos del usuario -->
                <label for="user-name">Name:</label> <!-- Etiqueta para el campo de nombre -->
                <input type="text" id="user-name" name="user-name" value="" required> <!-- Campo de entrada para el nombre, obligatorio -->
                
                <div class="button-container"> <!-- Contenedor para los botones de acción en el formulario -->
                    <button type="submit" class="save-button">Save</button> <!-- Botón para guardar los cambios -->
                    <button type="button" class="cancel-button">Cancel</button> <!-- Botón para cancelar la edición -->
                </div>
            </form>
        </div>

        <div class="button-container"> <!-- Contenedor para el botón de paginación -->
            <button class="next-button">Next</button> <!-- Botón para avanzar a la siguiente página de usuarios -->
        </div>
    </div>
</Layout>

<style>
    .list-container {
        background-color: #40d1e2;
        padding: 20px;
        border-radius: 5px;
        width: 100%;
        max-width: 600px;
    }
    .user-list {
        list-style-type: none;
        padding: 0;
    }
    .user-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
    }
    .user-name {
        color: #333;
    }
    .user-link {
        color: #0000EE;
        text-decoration: none;
        display: flex;
        align-items: center;
    }

    .container-buttons-list {
        display: flex;
        align-items: center;
    }

    .button-container {
        display: flex;
        justify-content: center;
    }

    .next-button {
        background-color: white;
        color: #333;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        margin: 20px auto auto auto;
    }

    .next-button:hover {
        background-color: #333;
        color: white;
    }

    .next-button:after {
        content: ">";
        margin-left: 5px;
    }

    /* Estilos para el formulario */
    #edit-form-container {
        background-color: white;
        padding: 20px;
        border-radius: 5px;
        max-width: 400px;
        margin-top: 20px;
    }

    .hidden {
        display: none;
    }

    .save-button, .cancel-button {
        background-color: white;
        color: #333;
        border: 1px solid #333;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        margin-right: 10px;
    }

    .save-button:hover, .cancel-button:hover {
        background-color: #333;
        color: white;
    }
</style>

<script>
   // Selecciona todos los enlaces de editar
    const editLinks = document.querySelectorAll('.edit-link');
    const editFormContainer = document.getElementById('edit-form-container');
    const userNameInput = document.getElementById('user-name') as HTMLInputElement; // Corregido aquí
    const cancelButton = document.querySelector('.cancel-button');

    editLinks.forEach(link => {
        link.addEventListener('click', function(event) {
            event.preventDefault();
            const userName = this.dataset.user;

            // Mostrar el formulario y rellenar el campo de nombre
            userNameInput.value = userName;  // Aquí se usa correctamente 'value'
            editFormContainer.classList.remove('hidden');
        });
    });

    // Manejar el botón de cancelar
    cancelButton.addEventListener('click', function() {
        editFormContainer.classList.add('hidden');
    });

</script>
